[package]
name = "interactive-examples"
version = "0.1.0"
edition = "2021"

[dependencies]
ply-engine = { path = "../../..", default-features = false, features = ["tinyvg", "text-styling"] }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
